# ── Cryptography ────────────────────────────────────────────────────
cryptography>=42.0.0        # AES-256-GCM via AESGCM
argon2-cffi>=23.1.0         # Argon2id key derivation

# ── Compression ─────────────────────────────────────────────────────
zstandard>=0.22.0            # Zstd (Python bindings for libzstd)

# ── Storage ─────────────────────────────────────────────────────────
boto3>=1.34.0                # AWS S3 / MinIO (S3-compatible)
botocore>=1.34.0             # boto3 dependency (explicit for pin)

# ── CLI ─────────────────────────────────────────────────────────────
click>=8.1.7                 # Command-line interface
rich>=13.7.0                 # Pretty tables and progress bars (optional)

# ── HTTP (webhook notifications) ────────────────────────────────────
requests>=2.31.0

# ── API (Sprint 4 — REST + WebSocket) ───────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # includes websockets + httptools
websockets>=12.0               # async WebSocket support
pydantic>=2.7.0                # schemas (FastAPI pulls this in; explicit pin)

# ── SSM: Windows VSS (optional, Windows only) ───────────────────────
# pywin32>=306              # Uncomment for richer VSS COM API support
#                           # Install: pip install pywin32 (Windows only)

# ── Scheduler (Sprint 6 — APScheduler + cron utils) ─────────────────
apscheduler>=3.10.4             # AsyncIOScheduler + CronTrigger
croniter>=2.0.1                 # Cron → next_run_at calculation
psutil>=5.9.0                   # Disk usage, CPU count (system info API)

# ── Testing ─────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0          # async test support
httpx>=0.27.0                   # ASGI transport for API smoke tests
anyio[trio]>=4.4.0              # async primitives (pytest-asyncio backend)
